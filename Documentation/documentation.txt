                      # SHORTCUTTR #



######################## Brief #########################

Essentially, the program sets an Alias to a directory, and stores them in a DB.
The DB will keep the Alias' constantly there unless the user decides to flush, or delete the file.

########################################################
######################## TODO ##########################

Largely finished, except for minor kinks and outputs.  
Flag | -fc | outputs text twice as it is being called always in Main() before everything else.  So maybe focus on a work-around on that.

Learn how to make a Manual page called by "man sc", as with every other command. <--# Done 

Display the Database with | sc -fs | in a nicer format

Make an installation version, and installon a different distro to test
  -so far, i can run it from the Main, but elsewhere it doesnt work.
  --it isn't finding the path to the Config, etc.

Get rid of that annoying fkn double slash in Path setting

Improve installer, and uninstaller - looks like shit.... add functions etc. for readability plus error checks

Improve some code, small things like error handling, flag handling, adding quotations to variables

Fix the issue of Unix incompatibilty - this is caused due to the differences between Gnu/Awk & normal Unix Awk like the one on Solaris.  
Maybe just find a way to not use awk! Would be easier.

Omg fix the function names if called "main", "init", "run", "build", etc.... any seemingly key words.  Could lead to bugs in the User's global 
environment due to the nature of the Program needing to be sourced in order to change directory in the current shell

Add functionality to list directories, then enter a number in the terminal corresponding to the Alias & Directory.

Make sure to add a trap SIGINT SIGTERM for the mktemp in function show_file() - it does not cleanup on ctrl+c;
  -watch -n 0.5 'tree /tmp/' shows the file is still there even on user termination, and I can cat the temp file and its contents.  
  --may need to account for error handling in the 'rm -f "$temp_file" but it should be fine' 

Fix that function checkFile_Secondary() that runs on everything including the help function on first use!
  -and fix that stupid fkn 'return 1' in the if statement... it's disgusting! How did I not see that before?!

Fix: sc -l -- firstly, mistake where exitScript.sh calls $$ instead of $SCRIPT_PID
  -the function does not exit on empty DB, it carries on into the 'read User-input';
  --fixed by using a return, I dunno why this wasn't working where it works in the uninstall script, but fixed nonetheless. 

Testing shows that if misused, and an Alias is called /var/ for example, I cannot delete it!
  -This could be a sed delimter issue, so maybe change that.  

Make it like more like: if an $alias is a directory path, then cd there.  Would also fix the issue in my db of '~' appearing as '/home/$(whoami);/home/$(whoami)' - which looks like shit in the DB - by blocking it from initial entry.
  -so if "sc /log/" it will just check cd first before resolving to aliases; this will also fix my issue of accidentally using sc instead of cd.    

Some better code refactoring/error handling!

^######################## All Done ########################^

Some even better code refactoring/error handling!

Why is the help function not in Alpahbetical order? sc -fd is after -ff! Fkn fix that! It's v annoying!

Come up with a better idea for backup and restore - because rn it backs-up after each entry, so maybe consider a secondary backup function in the background that will replace -fb so the user can backup to a different DB Backup at whatever point in time.
  -ah fuck, but then how do they restore? The restore file takes from the auto-backup.  Maybe make options for this, idk yet

Write out a test script for Containers - get the containers working first for a quick spin up with minimum config
  -this will become my way of testing from on in, then I can quickly spin-up a few containers and test automatically!

More testing!
  -need to fkn test on Mac, ask the Chairwoman for a lend of her Mac!
  -test more on other Distros, as well as in Terminal Emulators for the return and exits.  I haven't used an interactive terminal in ages, because I'm using Windows/WSL only these days!
  --test | sc -l where DB is empty |, | sc -fs, -fr, --uninstall | also to make sure those returns/exits dont shut down the terminal completely!

NEW FUNCTION: cleanupFile() 
  -just make it so that the User can call it, and not everytime! This is the best compromise
  --because doing what I wanted to do months ago by performing a cleanup task everytime a DB Edit is performed is too stringent, leave it to the User to control their own DB, not me!
  -perform a dry-run function that will tell the User before, then take User-input to continue/stop!



Maybe make it fully Bash so I can do full error-handling on everything!
  -E.g: 'sc -c "docs;fdsche"' will set the path to 'fdsche;/path/to/documents';
  --has bad handling when quotes are used, even though the script is meant to be used without it, but I'd rather account for it
  ---either full error/regex handling in Bash, or find a way to disable quotes being used... either way, probably best to give up on full compatibility with Shell.  Focus on Bash & zsh only. 


Bash completion and Zshell completion by tabbing - probably have to get Aliases, set to a tmp file - or a file somewhere - then call complete -F..... 
  - This will be probably different for Zshell.
  - Also do for the flags, but make sure that the Aliases are first to show.

Functioning Config file that the user can increase the speed of the printDelayedText function.  I imagine it drives some ppl crazy
and they may not know how to change it, or feel comfortable doing so.... then again, should I bother? I like it, plus: no one has offered any feedback,
and the Program has at least hundreds of unique clones.  Setting the limit to the -fs function would also make sense.

If an alias is deleted, '\nThis Directory doesn't exist!\n' is printed - so fkn fix that

Also, find a way to clean-up the db, when things are removed, it leaves new lines - maybe just delete new lines


##########################################################
######################## Notes ###########################

One thing to consider in the future - if Database is above a certain amount of rows, output with "less" instead of the current "cat".  "Cat" is more 
superior has it pertains to a smaller DB, with the DB dispalyed in the CLI for the User who won't have to rely on memory, and added shit such as having 
to press 'q'; but if a DB gets out-of-hand, it would be more logical to just use "less" (but how likely is it that I will need so many directories also?) 
Maybe wait to see if this effects me negatively enough in the future to where I need to consider it.  As well as adding extra flags to | -fs | like 
displaying just the tail, and the head of the DB. 


####################################################
################## How It Works #################### 

Flag:
            | -c | Create Shortcut <Alias Name> --e.g 'sc -c downloads';
            | -l | Lists all saved Shortcuts allowing the User to change directory based off the corresponding number entered in the terminal;
           | -fc | File Check - checks to see if the Database "Shortcut.txt" exists;
           | -fe | File Edit - brings the User into the Database via Nano for manual editing;
           | -ff | File Flush - flushes the Database on User request, so that new Alias' can be set;
           | -fd | File Delete - allows the user to delete the DB to reset it completely;
           | -fs | File Show - quickly displays the Database in the CLI for the User via Cat
           | -fr | File Restore - restores backup Database in case of User-Error;
        | --help | Displays all the flags for a User, and their relative functions;
       | <Alias> | This will bring the User to the relative Alias-set Directory;
   | --reinstall | Reinstalls the script again by calling the install script;
   | --uninstall | This will completely Uninstall the Program, and its data; 
  
So as stated above: by entering, for example, "sc -c downloads" in the User's Downloads folder, the Alias "downloads" will be entered next to the Directory with ";" as a delimter.

When the User, preferably from a different Directory, enters "sc downloads", the program will change the User to the specified Directory relative to the Alias name.

The Database, "shortcuts.txt" stores the User Alias', and can be Flushed, Deleted, Displayed, and more, by using "sc <flag>".  This allows the User's Alias' to be coninuously 
used, even after reboot.  |  -fe | will allow the User to manually edit the file, so if a mistake was made, the User won't have to delete everything to re-do one mistake.

Flags are named after the first letter of their functions.  This is why I set most of the flags dealing with the Database as | -fx |.  The letter "f" standing for "File", and 
the secondary letter acting as their function towards the File, e.g | -fe | being File Edit, and so on.  This nomenclature seemed most logical to me.  
